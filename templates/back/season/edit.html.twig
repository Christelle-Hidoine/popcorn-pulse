{% extends 'base.html.twig' %}

{% block title %}Edit Season{% endblock %}

{% block body %}

<div class="container-md bg-light my-5 border-top d-flex">
    <div class="row m-auto">

            <h1 class="text-muted my-5">Edit Season</h1>

            <div class="col-8">
                {{ include('back/season/_form.html.twig', {'button_label': 'Update'}) }}

                <div class="d-flex justify-content-end gap-3 mb-5 mt-3">
                    <a href="{{ path('app_back_season_index') }}" class="btn btn-warning btn-md review-btn mb-2" role="button" aria-disabled="true">Back to list</a>
                    {{ include('back/season/_delete_form.html.twig') }}
                </div> 
            </div>           
    </div>    
</div> 
{% endblock %}