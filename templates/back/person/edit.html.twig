{% extends 'base.html.twig' %}

{% block title %}Edit Person{% endblock %}

{% block body %}

<div class="container-md bg-light my-5 border-top d-flex">
        <div class="row m-auto">

            <h1 class="text-muted my-5">Edit Person</h1>
                <div class="col-9">
                    {{ include('back/person/_form.html.twig', {'button_label': 'Update'}) }}
                    
                    <div class="d-flex justify-content-start gap-3 mb-5 mt-3">
                        <a href="{{ path('app_back_person_index') }}" class="btn btn-warning btn-lg mb-2" role="button" aria-disabled="true">Back to list</a>
                    {{ include('back/person/_delete_form.html.twig') }}
                    </div>
            </div>
        </div> 
</div> 
{% endblock %}
