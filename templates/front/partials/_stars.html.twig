	<div class="d-flex mb-4" style="color: orange;">
		{% for i in 0..4 %}
			{% if ratingStars - i >= 0.9 %}
				<i class="bi bi-star-fill"></i>
			{% elseif ratingStars - i <= 0.1 %}
				<i class="bi bi-star"></i>
			{% else %}
				<i class="bi bi-star-half"></i>
			{% endif %}
		{% endfor %}
		<span class="ps-1">{{ ratingStars }}</span>
	</div>
