<ul class="navbar-nav ms-auto mb-2 mb-lg-0">
    <li class="nav-item dropdown">
        {% if  app.user  %}
            {% set user = app.user %}
        <a class="nav-link dropdown-toggle text-muted" href="#" id="navbarDropdown" role="button"
            data-bs-toggle="dropdown">
            <span>{{ user.userIdentifier }}</span>
            <i class="bi bi-person-square"></i>
        </a>
        <ul class="dropdown-menu">
            <li><a class="dropdown-item" href="#">{{ user.roles|first == 'ROLE_USER' ? 'Membre'}}</a></li>
            {% if is_granted('ROLE_MANAGER') %}
                <li><a class="dropdown-item" href="#">{{ user.roles|first == 'ROLE_ADMIN' ? 'Administrateur' : 'Manager' }}</a></li>
            {% endif %}
            {% set route = app.request.get('_route') %}
            {% if 'app_back' in route %}
                <li><a class="dropdown-item" href="{{ path('default')}}">Oflix</a></li>
            {% else %}
                <li><a class="dropdown-item" href="{{ path('app_back_default')}}">Backoffice</a></li>
            {% endif %}
            <li>
                <hr class="dropdown-divider">
            </li>
            <li><a class="dropdown-item" href="{{ path('app_logout') }}">Déconnexion</a></li>
        </ul>
    </li>
</ul>
        {% else %}

        <a class="nav-link dropdown-toggle text-muted" href="#" id="navbarDropdown" role="button"
            data-bs-toggle="dropdown">
            <span>Visiteur</span>
            <i class="bi bi-person-square"></i>
        </a>
        <ul class="dropdown-menu">
            <li><a class="dropdown-item" href="#">Visiteur</a></li>
        </ul>
    </li>
</ul>

<button type="button" class="btn btn-danger me-2" onclick="window.location.href = '{{ path('app_login') }}';">Connexion</button>
{% endif %}